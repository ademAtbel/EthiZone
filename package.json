{
  "name": "ethizone-marketplace",
  "version": "1.0.0",
  "description": "EthiZone direct-connect marketplace connecting individuals, handymen, stores, and customers.",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix client",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "lucide-react": "^1.24.0"
  }
}
