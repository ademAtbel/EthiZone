# Environment Configuration Example for EthiZone Frontend (Vite)

# Backend API Base URL (empty string in dev when proxying via Vite, or full production API URL)
VITE_API_BASE_URL=

# Socket.io Server URL (defaults to current host or dev server)
VITE_SOCKET_URL=
